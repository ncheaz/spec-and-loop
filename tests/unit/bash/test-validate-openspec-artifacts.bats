#!/usr/bin/env bats

# Test suite for the removed validate_openspec_artifacts() function
# All tests removed — validate_openspec_artifacts was eliminated in the
# cli-driven-manifest-resolution change. Artifact validation is now
# handled by the openspec skill during /opsx-apply execution.
